# CoCo 설계 결정 기록

구조를 왜 이렇게 잡았는지 남기는 문서다. `RESULT.md`는 시간순 작업 기록이라 판단 근거가 여러 항목에 흩어지므로, 나중에 "왜 이렇게 되어 있지?"라는 질문이 생길 만한 결정만 여기에 모은다.

각 항목은 **상황 → 결정 → 대안 → 결과** 순으로 적는다. 결정이 뒤집히면 항목을 지우지 말고 아래에 뒤집은 이유와 함께 갱신한다.

최근 갱신: 2026-08-17

---

## 1. 경로 계산

### D1. 구간 결과를 캐싱하고 합치기는 매번 다시 한다

**상황.** `MKDirections.Request`는 출발지와 도착지만 받는다. 경유지를 한 번에 넘길 수 없어 지점 N개짜리 경로는 요청 N-1회로 쪼개야 한다. 초기 구현은 지점이 바뀔 때마다 전체를 다시 계산했고, 결과인 `PlannedRoute`만 보관하고 구간별 응답은 버렸다.

**결정.** 구간별 결과를 `[RouteSegmentState]`로 보관한다. 지점 추가는 새 구간 1개만 요청하고, 되돌리기는 캐시에서 마지막 항목만 제거해 요청이 발생하지 않는다. 반대로 구간을 이어붙이고 누적 거리를 계산하는 **합치기는 로컬 연산이라 캐시가 바뀔 때마다 다시 한다.**

**대안.** (1) 전체 재계산 유지 — 상태 관리가 단순하고 항상 정확하다. (2) 합쳐진 결과만 증분 갱신 — 누적 거리 재계산이 까다롭고 구간 단위 실패를 표현할 수 없다.

**결과.** 지점 25개까지 찍는 동안 누적 요청이 300회에서 24회로 줄었다. 비싼 작업(네트워크)과 싼 작업(로컬 병합)을 분리한 것이 핵심이고, 캐시와 지점 목록의 길이를 맞추는 동기화 책임이 새로 생겼다. 이 구조가 D2의 전제이기도 하다.

### D2. 구간 실패는 전체 실패가 아니다

**상황.** 초기 구현은 한 구간이라도 실패하면 경로 전체를 `.failed`로 만들어 이미 받은 구간까지 화면에서 사라졌다. 지점이 25개면 24개 중 하나만 실패해도 전부 버리는 셈이다.

**결정.** 구간마다 `pending / resolved / failed` 상태를 따로 갖는다. 실패한 구간은 표시만 하고 나머지 구간은 계속 계산한다. 재시도는 실패한 구간만 다시 요청한다. 구간당 최대 3회 지수 백오프로 재시도하되, `MKError.loadingThrottled`는 요청 폭주가 원인이므로 더 긴 대기를 적용한다.

**대안.** 실패 시 즉시 중단 — 구현은 단순하지만 사용자가 어느 지점이 문제인지 알 수 없고 이미 성공한 작업을 잃는다.

**결과.** 사용자가 문제 지점만 옮기거나 지워서 복구할 수 있다. 상태 조합이 늘어 UI가 다뤄야 할 경우의 수가 많아졌다.

### D3. 지도에 없는 경로는 그리지 않는다

**상황.** 구간 일부만 성공한 상태에서 성공 구간을 하나로 이어 그리면, 실패한 구간 자리에 실제로는 존재하지 않는 직선이 생긴다.

**결정.** 연속으로 성공한 구간만 묶어 실선으로 그린다(`resolvedPolylines`). 실패한 구간은 두 지점 사이를 빨간 점선으로 따로 표시한다.

**대안.** 성공 구간을 전부 이어 하나의 선으로 그리기 — 코드가 단순하지만 사용자에게 거짓 정보를 준다.

**결과.** 화면이 실제 상태를 그대로 반영한다. 코스 판단을 돕는 것이 이 앱의 핵심 가치이므로 이 원칙을 우선한다.

### D4. 경로 계산기를 프로토콜로 분리한다

**상황.** 캐싱의 효과는 "요청 횟수가 줄었다"인데, MapKit을 직접 호출하면 이를 검증할 방법이 없다.

**결정.** `WalkingRouteCalculator` 프로토콜을 두고 `MapKitWalkingRouteCalculator`를 주입한다. 테스트는 호출 횟수를 세는 스텁이나 특정 구간만 실패시키는 스텁을 넣는다.

**대안.** 통합 테스트로 실제 MapKit 호출 — 네트워크에 의존하고 느리며 호출 횟수를 관찰할 수 없다.

**결과.** "지점 6개 추가 = 요청 5회", "재시도 시 실패 구간만 1회 추가" 같은 성질을 숫자로 검증한다. 테스트 전체가 네트워크 없이 돈다. 요청 제한 측정처럼 실제 호출이 필요한 경우에는 임시 측정 코드를 쓰고 측정 후 제거한다.

### D5. 자유 그리기는 막지 않고 드러낸다

**상황.** 사용자가 지도에 직접 그린 경로는 강이나 건물 위를 지날 수 있다. 반대로 지도에 없는 숲길과 지름길을 표현할 수 있다는 것이 이 기능의 존재 이유다.

**결정.** 도로 스냅이나 검증으로 막지 않는다. 대신 `routeSource`를 `DRAWN_FREEHAND`로 구분해 저장하고, 코스 카드와 상세에 출처 배지를 표시한다. 저장 전 위성 지도로 확인할 수 있게 한다. 노면과 통행 조건에 대한 구체적 주의는 `CAUTION` 요소로 남긴다.

**대안.** 그린 선을 도로망에 스냅 — 잘못된 경로는 막히지만 숲길을 그릴 수 없어 기능의 목적 자체가 사라진다.

**결과.** 정보를 숨기지 않고 다른 러너가 판단하게 한다. 품질이 낮은 경로가 등록될 수 있고, 필요해지면 신고 장치를 따로 검토한다.

---

## 2. 클라이언트 구조

### D6. 공용 모델에 지도 SDK 타입을 넣지 않는다

**상황.** 좌표를 다루는 모델이 앱 전체에 퍼진다.

**결정.** `Course`, `RoutePoint`, `CourseElement` 같은 공용 모델은 `CLLocationCoordinate2D`를 저장하지 않고 자체 좌표 타입을 쓴다. 변환은 MapKit 경계(`MapCanvasView`, `RegisterView`)에서만 한다. 지도 영역 필터도 SDK에 의존하지 않는 `MapViewport`로 표현한다.

**대안.** MapKit 타입을 그대로 사용 — 변환 코드가 없어 편하지만 서버 계약과 도메인 로직이 지도 SDK에 묶인다.

**결과.** 모델이 `Codable`이고 테스트가 지도 없이 돈다. 경계에서 변환 코드가 필요하다.

### D7. 하프시트를 네이티브 `.sheet` 대신 커스텀 패널로 만든다

**상황.** 탐색 화면은 지도 위에 코스 목록을 겹쳐 보여야 한다. 모달 `.sheet`는 탭 바를 가려 최상위 탭 이동을 막는다. 사용자가 원한 동작은 두 단계 전환이 아니라 원하는 높이에 멈추는 free-stop이고, 시트 높이와 무관하게 목록이 스크롤돼야 한다.

**결정.** 탐색 탭 내부의 커스텀 바텀 패널로 구현한다. 그래버와 헤더 드래그로 높이를 자유롭게 조절하고, 최소 높이 아래로는 닫히지 않는다.

**대안.** `.presentationDetents` — 시스템 동작을 그대로 얻지만 탭 바를 가리고 임의 높이 정지를 지원하지 않는다.

**결과.** 원하는 상호작용을 얻었고 시트 크기 계산과 제스처 처리를 직접 맡는다. Dynamic Type에 따라 높이를 배율로 조정한다.

### D8. 지도와 시트의 높이는 서로 독립이다

**상황.** 지도 하단 여백을 시트 높이에 연동했더니, 시트를 올릴 때마다 지도 전체가 밀려 올라가 핀과 범례가 함께 이동했다.

**결정.** 지도의 하단 여백을 시트의 **최소 높이로 고정**한다. 시트를 올리면 지도를 덮는다.

**대안.** 연동 유지 — 지도가 항상 보이지만 사용자가 보던 지점이 계속 움직인다.

**결과.** 시트를 조작해도 지도가 흔들리지 않는다. Apple Maps와 같은 동작이다.

### D9. 겹치는 표면은 지도, 시트, 탭 바 셋으로 제한한다

**상황.** 요소 상세를 화면에 떠 있는 딤 처리된 카드로 띄웠더니 탭 바, 하프시트, 모달 카드가 하단에서 경쟁했다.

**결정.** 요소 상세를 하프시트 콘텐츠로 옮긴다. 시트 헤더가 뒤로 가기와 요소 제목으로 바뀌고, 좌우 스와이프로 같은 코스의 다른 요소로 이동한다.

**대안.** 별도 시트나 팝오버 — 레이어가 하나 더 늘고 한 번에 하나의 시트라는 원칙에 어긋난다.

**결과.** 딤 레이어가 사라지고 하단 구성이 단순해졌다. 시트가 목록과 상세라는 두 모드를 갖게 됐다.

### D10. 목록 필터는 클라이언트에서 한다

**상황.** "이 지도 영역에서"라는 문구와 달리 목록은 항상 전체 코스를 보여줬고 검색도 없었다.

**결정.** 지도 영역 겹침 판정과 텍스트 검색을 모두 클라이언트에서 수행한다. 선택된 코스는 영역 밖으로 나가도 목록에 유지한다.

**대안.** 서버 쿼리로 필터 — 코스 수가 많아지면 필요하지만 지금 규모에서는 왕복이 늘고 지도 이동마다 요청이 나간다.

**결과.** 서버 변경 없이 즉시 반응한다. 코스 수가 크게 늘면 서버 페이지네이션과 함께 다시 설계해야 한다.

---

## 3. 계정과 인증

### D11. 로그인 화면 없이 게스트로 시작한다

**상황.** 첫 실행에서 로그인을 요구하면 지도를 보기까지 마찰이 생긴다.

**결정.** 첫 실행 시 서버가 게스트 계정과 토큰을 발급하고, 토큰은 Keychain에 저장한다. 로그인은 나중에 선택적으로 연결한다.

**대안.** 로그인 필수 — 계정 복구가 쉬워지지만 진입 장벽이 생긴다.

**결과.** 즉시 사용 가능하다. 앱 삭제나 기기 변경 시 게스트 데이터는 복구되지 않으며, 이를 해결하는 것이 D13이다.

### D12. 소셜 로그인의 코드 교환은 서버가 한다

**상황.** OAuth 클라이언트 비밀값이 앱 번들에 들어가면 추출될 수 있다.

**결정.** 앱은 인가 코드만 받아 서버로 넘기고, 서버가 제공자 토큰 엔드포인트와 프로필 API를 호출한다. 인가 URL도 서버가 만들어 주므로 앱에는 `client_id`조차 없다. 제공자 액세스 토큰은 사용자 식별자 조회에만 쓰고 저장하지 않는다. 인가 단계는 외부 SDK 없이 iOS 네이티브 `ASWebAuthenticationSession`으로 수행하고 `state`로 CSRF를 검증한다.

**대안.** (1) 제공자 SDK 도입 — 앱 간 전환 UX가 낫지만 외부 의존과 앱 내 키 관리가 생긴다. (2) 앱에서 직접 코드 교환 — 비밀값이 노출된다.

**결과.** 비밀값이 서버에만 있고 제공자 설정이 한곳에 모인다. 두 제공자가 리디렉션 주소를 `https`로 제한하므로 서버 콜백이 커스텀 스킴으로 넘겨주는 단계가 필요하다.

### D13. 계정 유형은 제공자 이름이 아니라 게스트/회원으로 둔다

**상황.** 처음 스키마는 `account_type`이 `GUEST`/`APPLE`이었다. 제공자를 이름으로 박으면 제공자가 늘 때마다 값이 늘고, 한 계정에 여러 제공자를 연결할 수 없다.

**결정.** `account_type`은 `GUEST`/`MEMBER`만 갖고, 어떤 제공자가 연결됐는지는 `external_identities` 테이블과 응답의 `linkedProviders`로 표현한다. `(provider, provider_user_id)`에 유일 제약을 둔다.

**대안.** `GUEST`/`NAVER`/`KAKAO` — 단순하지만 다중 연결을 못 하고 제공자 추가가 스키마 변경이 된다.

**결과.** 제공자 추가가 데이터 추가로 끝난다. 계정 병합 규칙은 아직 정하지 않았다.

### D14. 이미 연결된 신원은 게스트 데이터를 흡수하지 않는다

**상황.** 게스트 상태에서 이미 다른 계정에 연결된 소셜 계정으로 로그인하면 두 계정의 데이터를 어떻게 할지 정해야 한다.

**결정.** 세 가지 규칙으로 나눈다. (1) 외부 신원이 이미 어떤 사용자에 연결됨 → 그 계정으로 로그인하고 게스트 데이터는 병합하지 않는다. (2) 미연결 + 유효한 게스트 토큰 → 그 게스트를 `MEMBER`로 승격하고 데이터를 그대로 유지한다. (3) 미연결 + 게스트 없음 → 새 계정을 만든다. 승계는 하나의 트랜잭션에서 처리한다.

**대안.** 자동 병합 — 편리해 보이지만 서로 다른 사람의 데이터가 합쳐질 위험이 있고 되돌릴 수 없다.

**결과.** 예측 가능하고 안전하다. 의도적으로 두 계정을 합치려는 사용자는 지금 방법이 없다.

---

## 4. 서버

### D15. 소유자는 항상 토큰에서 결정한다

**상황.** 코스와 요소는 작성자만 수정·삭제할 수 있어야 한다.

**결정.** 요청 본문의 소유자 값을 아예 받지 않는다. 서버가 인증 토큰에서 현재 사용자를 결정하고, 권한 위반은 `403 COURSE_OWNER_ONLY`로 거부한다.

**대안.** 클라이언트가 보낸 `ownerId` 신뢰 — 위조로 남의 코스를 조작할 수 있다.

**결과.** 클라이언트를 신뢰하지 않는 경계가 명확하다.

### D16. 계정 삭제는 코스를 먼저 지운다

**상황.** `courses.owner_id`가 `ON DELETE RESTRICT`라서 코스를 가진 사용자는 DB 레벨에서 삭제가 막힌다.

**결정.** 계정 삭제 시 사용자의 코스를 먼저 명시적으로 삭제한 뒤 사용자를 삭제한다. 전부 하나의 트랜잭션에서 수행한다. 코스 삭제는 경로 지점, 요소, 그 코스에 달린 모든 사용자의 스크랩·반응으로 연쇄되고, 사용자 삭제는 토큰, 외부 신원, 그 사용자가 남긴 스크랩·반응으로 연쇄된다.

**대안.** `owner_id`를 `ON DELETE CASCADE`로 바꾸기 — 코드가 단순해지지만 실수로 사용자를 지울 때 코스가 조용히 사라진다.

**결과.** 삭제 순서가 코드에 드러나고 실수로 인한 연쇄 삭제를 막는다.

### D17. 토큰은 해시만 저장한다

**결정.** 인증 토큰 원문은 발급 응답에서 한 번만 전달하고 서버에는 SHA-256 해시만 저장한다. 로그아웃은 해당 토큰만 폐기하고 다른 기기의 세션은 유지한다. 소셜 로그인으로 게스트를 승격할 때는 승격에 쓰인 게스트 토큰을 폐기한다.

**결과.** DB가 유출돼도 토큰을 재사용할 수 없다.

---

## 5. 인프라와 배포

### D18. 공개 트래픽과 운영 트래픽을 분리한다

**상황.** 처음에는 Tailscale로 앱 트래픽까지 처리하려 했으나, 그러면 모든 사용자가 같은 VPN에 들어와야 해서 공개 앱 구조로 성립하지 않는다.

**결정.** 일반 앱 요청은 Cloudflare Tunnel의 HTTPS 호스트로 받고, Tailscale은 개발자 SSH와 CI/CD 배포 경로로만 쓴다. Mac mini에는 공개 인바운드 포트를 두지 않는다. Actuator는 별도 관리 포트를 루프백에만 바인딩해 공개 호스트에서 접근할 수 없게 한다.

**대안.** 둘을 직렬로 묶기 — 구성이 복잡해지고 역할이 섞인다.

**결과.** 역할이 명확하고 공개 표면이 최소다.

### D19. 배포 SSH는 정해진 명령만 실행한다

**상황.** CI가 서버에 접근하려면 SSH 키가 필요한데, 그 키가 유출되면 서버 전체를 조작할 수 있다.

**결정.** GitHub 전용 키는 `authorized_keys`의 강제 명령으로 묶여 `deploy sha-<40자 커밋>`과 `sync-ops` 두 가지만 실행할 수 있다. 배포는 커밋 SHA로 고정된 이미지만 받고, 이미지의 revision 라벨을 대조한 뒤 헬스 체크가 실패하면 이전 digest로 자동 롤백한다.

**대안.** 일반 SSH 접근 허용 — 편리하지만 키 유출 시 피해가 서버 전체로 번진다.

**결과.** CI가 할 수 있는 일이 좁게 정해져 있다.

### D20. 운영 파일 동기화의 허용 목록은 서버가 갖는다

**상황.** Mac mini는 GHCR 이미지만 자동으로 받고 `compose.production.yaml` 같은 운영 파일은 수동이었다. 저장소에서 고친 설정이 서버에 반영되지 않아 소셜 로그인 환경변수가 컨테이너에 전달되지 않는 문제가 실제로 발생했다.

**결정.** 배포 job이 운영 파일을 tar로 보내고, Mac mini의 `sync-ops.sh`가 **자체 허용 목록에 있는 파일만** 설치한다. 새 compose 파일은 현재 비밀값으로 `docker compose config` 검증을 통과해야 하며, 실패하면 기존 파일을 유지한다. 호스트에서 실행되는 스크립트와 systemd 유닛은 배포 경로의 보안 경계 자체이므로 목록에서 제외하고 수동으로 남긴다.

**대안.** (1) 수동 유지 — 오늘 같은 어긋남이 반복된다. (2) 무엇이든 덮어쓰기 — main에 푸시할 수 있는 사람이 호스트에서 임의 코드를 실행할 수 있게 된다.

**결과.** 설정 드리프트가 사라지면서도 무엇을 덮어쓸 수 있는지는 서버가 통제한다.

### D21. 저장소는 용도에 따라 나눈다

**상황.** 사용자 사진은 자주 조회돼 외부 전송량이 크고, DB 덤프는 거의 읽지 않는다. 클라우드 스토리지 비용은 대부분 전송 요금에서 나온다.

**결정.** 사용자 사진은 전송 요금이 없는 Cloudflare R2에, DB 덤프와 아카이브는 저비용 클래스를 쓰는 AWS S3에 둔다. 둘 다 S3 호환 API라 서버는 하나의 클라이언트 추상화로 다룬다. 사진은 R2가 원본이고, R2 자체 유실에 대비해 야간에 S3로 단방향 동기화한다.

**대안.** (1) S3 단독 + CDN — 같은 효과를 얻지만 CDN 구성이 추가된다. (2) R2 단독 — 오프사이트 백업 대상이 없어진다.

**결과.** 비용 구조가 데이터 성격과 맞는다. DB 덤프에는 사진 바이트가 없으므로 사진 백업을 따로 두는 것이 필수다.

### D22. 비밀값은 저장소에 두지 않고 이름만 남긴다

**결정.** 실제 값은 세 곳에만 있다. 로컬 `.env`(개발용 더미), GitHub Actions Secrets(배포 키와 Tailscale OIDC), Mac mini `.env.production`(DB 비밀번호, Cloudflare 터널 토큰, 소셜 로그인 자격 증명). 저장소에는 키 이름과 설명만 담은 `.env.example`, `.env.production.example`을 둔다.

**결과.** 저장소가 공개돼도 비밀값이 노출되지 않는다. 대신 서버의 `.env.production`은 수동 관리이며, 예시 파일에 항목을 추가해도 서버 파일에는 자동으로 반영되지 않는다.

### D24. 사진 바이트는 서버를 통과하지 않는다

**상황.** 요소 사진은 장당 수 MB다. Mac mini 한 대가 업로드 트래픽을 중계하면 Cloudflare Tunnel과 Tomcat 스레드를 사진이 오래 점유한다.

**결정.** 서버는 소유권과 형식·크기 한도만 검사하고 사전 서명 PUT URL을 발급한다. 클라이언트가 저장소에 직접 올리고, 끝나면 확정 요청을 보낸다. 서버는 확정에서 객체의 존재와 크기를 다시 확인한 뒤에만 DB에 키를 기록한다.

**대안.** (1) 멀티파트 업로드를 서버가 받아 저장소로 중계 — 구현은 단순하지만 홈서버 대역폭과 스레드를 사진이 잡아먹는다. (2) 발급만 하고 확정을 두지 않음 — 요청 본문의 크기를 믿게 되어 한도가 무의미해진다.

**결과.** 사진 크기가 서버 처리량에 영향을 주지 않는다. 대신 상태가 두 곳(저장소와 DB)에 나뉘어, 업로드는 됐지만 확정되지 않은 객체가 생길 수 있다. 저장소 수명 주기 규칙으로 정리한다.

### D25. 객체 키는 업로드마다 새로 만들지 않는다

**상황.** SPEC 초안은 업로드마다 UUID를 붙이는 키였다. 그러면 확정에 실패한 시도마다 가리키는 것 없는 객체가 하나씩 남는다.

**결정.** 키를 `courses/{courseId}/elements/{elementId}.{jpg|heic}`로 요소·형식당 하나로 고정한다. 같은 형식으로 다시 올리면 덮어써지고, 형식이 바뀌는 교체에서만 이전 객체가 남으므로 확정 시점에 그것만 명시적으로 지운다.

**대안.** 업로드마다 새 키 + 수명 주기 규칙에 정리를 맡김 — 정리까지의 기간 동안 비용과 잔여 객체가 쌓인다.

**결과.** 잔여 객체가 구조적으로 거의 생기지 않는다. 대신 키가 예측 가능해지므로 조회 경로는 반드시 사전 서명 URL이어야 하고 버킷을 공개하면 안 된다.

### D26. 저장소 자격 증명이 없어도 서버는 뜬다

**상황.** 개발과 테스트 환경에는 R2 자격 증명이 없다. 부팅 시점에 검증하면 사진과 무관한 작업까지 서버를 띄우지 못한다.

**결정.** 자격 증명이 없으면 모든 호출을 거부하는 구현을 대신 주입하고, 사진 API만 `503 PHOTO_STORAGE_UNAVAILABLE`로 응답한다.

**대안.** 부팅 시 필수 검증 — 설정 누락을 가장 빨리 발견하지만 개발 환경마다 자격 증명을 요구하게 된다.

**결과.** 개발이 저장소 설정에 묶이지 않고, 운영에서 설정이 빠지면 해당 기능만 명확한 이유와 함께 꺼진다. 대신 운영 설정 누락이 기동 실패가 아니라 런타임 오류로 드러나므로 배포 후 사진 경로를 한 번 확인해야 한다.

### D27. 사진 조회는 사전 서명 URL로 하고 캐시는 업로드 시각으로 잡는다

**상황.** 사진 바이트를 앱까지 전달하는 방법은 네 가지가 가능했다. 어느 쪽이든 저장소는 R2이고, 차이는 저장소가 요청을 통과시키는 근거와 URL이 고정인지 여부다.

| 방법 | 바이트 경로 | 버킷 | URL | 앱 캐시 |
|---|---|---|---|---|
| (1) 서버 중계 | 앱 ↔ 스프링 ↔ R2 | 비공개 | 고정 | 적중 |
| (2) 사전 서명 GET | 앱 ↔ R2 | 비공개 | 서명·시각 포함, 계속 바뀜 | 항상 빗나감 |
| (3) 커스텀 도메인 | 앱 ↔ Cloudflare 엣지 ↔ R2 | 사실상 공개 | 고정 | 적중 |
| (4) 사전 서명 GET + 내용 기준 캐시 | 앱 ↔ R2 (캐시 미스일 때만) | 비공개 | 계속 바뀜 | 적중 |

(1)은 D24에서 배제했다. 맥미니 한 대가 수 MB 전송을 중계하면 Tomcat 스레드와 터널 대역폭을 사진이 점유한다.

(2)만으로는 캐시가 동작하지 않는다. iOS `URLCache`는 쿼리를 포함한 URL 전체를 캐시 키로 쓰는데, 사전 서명 URL에는 서명 시각(`X-Amz-Date`)과 서명이 들어 있어 응답마다 달라진다. 같은 사진을 목록을 열 때마다 다시 내려받게 된다. 비용 문제는 아니다. R2는 egress가 무료이고 읽기 연산도 무료 한도에 한참 못 미친다. 문제는 로딩 지연과 사용자 데이터 요금이다.

(3)은 URL이 고정이라 캐시가 그냥 동작하지만 버킷이 공개된다. 객체 키를 요소·형식당 하나로 고정했기 때문에(D25) 키는 `courseId`와 `elementId`로부터 조합할 수 있고, 이 둘은 API 응답에 그대로 들어 있다. 즉 코스를 한 번 본 사람은 그 사진을 영구히 조회할 수 있게 된다.

**결정.** (4)를 택한다. 조회는 유효 1시간의 사전 서명 GET URL로 하고 버킷은 비공개로 둔다. 대신 앱이 `URLCache`에 기대지 않고 `elementId + photoUploadedAt`을 키로 직접 캐시한다. 이를 위해 요소 응답에 `photoUploadedAt`을 함께 내려준다.

캐시 키가 성립하는 근거는 이 값이 **사진이 실제로 바뀔 때만 바뀐다**는 것이다. 사진을 교체하면 확정 시각이 새로 기록되어 키가 달라지고 옛 파일은 자연히 무시된다.

**결과.** 캐시 적중률은 (3)과 같으면서 버킷은 비공개로 남는다. 커스텀 도메인, DNS, 엣지 캐시 규칙 같은 인프라가 늘지 않는다. 서명 만료도 문제가 되지 않는다. 캐시에 있으면 URL을 쓰지 않고, 미스일 때만 코스를 다시 불러 새 URL을 받는다.

대신 캐시 관리를 앱이 직접 진다. 용량 상한과 축출 정책을 우리가 정해야 하고, `URLCache`가 공짜로 주던 것을 코드로 갖게 된다. 사진 수가 늘어 엣지 캐시가 분명히 유리해지면 (3)으로 옮길 수 있다. 그때는 키를 예측 불가능하게 바꾸는 일이 함께 필요하다.

### D28. 사진은 요소가 만들어진 뒤에 올라간다

**상황.** 사진은 특정 요소에 붙는다. 그런데 코스 등록은 코스와 요소를 한 번의 요청으로 만들기 때문에, 등록을 마치기 전에는 요소 식별자가 없다. 사진을 먼저 올릴 대상이 존재하지 않는다.

**결정.** 초안은 고른 사진을 가공된 바이트로 들고만 있고, 요소가 저장된 뒤에 업로드한다. 코스 등록에서는 응답으로 받은 요소들과 초안을 거리 순으로 짝지어 올리되, 각 쌍이 제목과 거리로 일치하는지 확인한 뒤에만 보낸다.

**대안.** (1) 사진 없는 요소를 먼저 만들고 사진을 올린 뒤 나머지를 채우기 — 실패하면 빈 요소가 남는다. (2) 사진을 다중 파트로 등록 요청에 함께 싣기 — 바이트가 서버를 통과하게 되어 D24와 어긋난다.

**결과.** 텍스트는 저장됐는데 사진만 실패하는 상태가 생길 수 있다. 코스를 되돌리는 대신 그대로 두고 사용자에게 알린 뒤 요소 수정에서 다시 시도하게 한다. 짝짓기 확인 덕분에 최악의 경우가 "사진이 안 붙음"이지 "다른 요소에 붙음"이 아니다.

---

## 6. 문서 운영

### D23. 범위 변경은 코드보다 SPEC을 먼저 고친다

**결정.** 새 기능은 `BACKLOG.md`에 후보로 적고, 구현 전에 `SPEC.md`의 확정 범위로 옮기면서 기준과 확인 시나리오를 정의한다. 구현 기록은 `RESULT.md`, 개념 학습은 `STUDY.md`, 설계 판단은 이 문서에 남긴다.

**결과.** 구현과 문서가 어긋나지 않는다. 문서 갱신 비용이 매 단계 발생한다.

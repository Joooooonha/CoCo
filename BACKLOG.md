# CoCo Backlog

사용자가 기록한 후속 기능 후보와 개선 항목. 여기 있는 항목은 아직 확정 범위가 아니다.
구현을 시작하기 전에 `SPEC.md`의 MVP 또는 다음 버전 확정 범위로 옮기고 세부 기준을 정의한다.

작성일: 2026-08-13

---

## 1. 새로 추가할 기능

### B1. 앱 안에서 GPX 경로 생성
현재는 카카오맵 등 외부 서비스에서 경로를 만든 뒤 GPX 파일로 가져오는 방식이다.
앱 안에서 직접 세밀한 경로를 그려 GPX 수준의 결과를 만들 수 있어야 한다.

- 관련: B7(코스 자체 생성), B6(경로 결정 기준)
- 참고: 현재 MapKit 계획 경로는 지점 7개 상한

### B2. GPS 기록 기반 코스 생성
Apple Watch 또는 iPhone GPS로 실제로 달린 경로를 기록해 코스로 만든다.

- `RouteSource.RECORDED_GPS` 값은 이미 데이터 모델에 예약되어 있다
- 위치 권한, 백그라운드 위치 수집, 배터리 정책을 함께 결정해야 한다
- 현재 `SPEC.md`는 위치 권한 요청을 명시적으로 제외하고 있으므로 스코프 변경이 필요하다

### B3. 동적 날씨 표현
노을, 비, 눈 등 현재 날씨 상태를 지도에 동적으로 표시한다. 태양과 구름이 실제로 움직이는 표현.

- 우선순위: 가장 마지막
- WeatherKit 사용 가능성 검토 필요
- 성능과 배터리 영향, Reduce Motion 대응을 함께 확인한다

### B4. 커뮤니티 / 크루
러닝 크루 생성과 모집 기능.

- 사용자 간 관계, 가입 승인, 신고와 차단 정책이 함께 필요하다
- 현재 `SPEC.md`의 스코프 제외 항목(댓글, 팔로우, 소셜 피드)과 겹치므로 범위 재정의 필요

### B5. 코스 안내 (내비게이션)
선택한 코스를 따라 실시간 안내를 제공한다.

- 음성 안내: "10m 앞 우회전" 형태
- 실시간 위치와 동선 표시
- 경로 이탈 감지와 알림
- 참고: 네이버 지도 길 안내 수준의 경험
- 보행자 턴바이턴 안내 API 확보 방안 확인 필요 (카카오 도보 API는 제휴 전용)

---

## 2. 완성도를 높일 기존 기능

### B6. 지도 API 경로 결정 기준 확인
코스 생성 시 지도 API가 어떤 기준으로 경로를 정하는지 확인해야 한다.

- 최단 거리 기준인지, 다른 최적화 기준인지 불명확
- 사용자가 직진을 원해도 API가 다른 경로를 선택하는 문제 확인됨
- 사용자 의도와 API 최적 경로가 어긋날 때의 대응 방식 결정 필요

### B7. 코스 자체 생성 개선
GPX처럼 원하는 지점을 세밀하게 지정할 수 있어야 한다.

- 현재 제약: 지점 7개 상한 (출발 + 경유 5 + 도착)
- 자유 곡선 그리기 또는 지점 수 확대 방식 검토
- 관련: B1, B6

### B8. 사진 및 영상 삽입
요소에 사진과 영상을 첨부한다. 현재는 자리 표시만 있다.

- S3 또는 동등한 오브젝트 스토리지 연동 필요
- 비용, 인증, 삭제 정책을 함께 결정한다 (`SPEC.md` 12장 미결정 사항)
- 영상은 용량과 트랜스코딩 정책을 별도로 확인한다

### B9. 로그인과 개인 계정 관리
현재는 로그인 화면 없는 게스트 계정만 있다.

- Sign in with Apple은 이미 V2 확정 범위 (V2-F2, V2-F3, V2-F4)
- 게스트 계정 승계, 로그아웃, 계정 삭제 포함
- 프로필 화면 구성은 아직 미정 (현재는 보관함의 이름 변경만 존재)

### B10. 전체적인 UI 개선
전반적인 시각 완성도와 일관성 향상.

- Figma 시안 대비 시각 보정 (`SPEC.md` Phase 7)
- Dynamic Type, VoiceOver, 대비 점검
- 세부 항목은 별도 정리 필요

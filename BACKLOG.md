# CoCo Backlog

사용자가 기록한 후속 기능 후보와 개선 항목.

여기 있는 항목은 확정 범위가 아니다. 구현을 시작하기 전에 `SPEC.md`의 확정 범위로 옮기고 세부 기준을 정의한다.

작성일: 2026-08-13
최근 갱신: 2026-08-13 (B6~B10을 SPEC V2 범위로 이관)

---

## 진행 순서

사용자 결정에 따라 **기존 기능의 완성도(B6~B10)를 먼저** 끝내고, 그다음 새 기능(B1~B5)의 범위를 다시 확정한다.

---

## 1. SPEC으로 이관 완료 (V2 확정 범위)

아래 항목은 `SPEC.md`에 기준이 정의되어 구현 대기 상태다. 세부 내용은 SPEC을 따른다.

| 백로그 | SPEC 위치 | 비고 |
|---|---|---|
| B6. 지도 API 경로 결정 기준 확인 | 3.4 경로 계산 공급자 제약 | 조사 완료. MapKit은 경로 기준 조절 API가 없고, 지점을 촘촘히 찍는 것이 유일한 수단 |
| B7. 코스 자체 생성 개선 | 3.4 V2 정밀 경로 계획 / V2 자유 그리기 경로, V2-F9·F10, Phase 9 | 지점 상한 25개 + 자유 그리기 병행 |
| B8. 사진 삽입 | 3.5 V2 요소 사진, 6.6 객체 저장소, 7.3 업로드 계약, V2-F11, Phase 10 | 사진만. 저장소는 R2(서비스) + S3(백업) 분리 |
| B9. 로그인과 개인 계정 관리 | V2-F2·F3·F4·F12, Phase 8 | 인증은 기존 V2 범위, 계정 화면을 F12로 추가 |
| B10. 전체적인 UI 개선 | V2-F13, Phase 11 | 화면별 점검표로 관리 |

### B6 조사 결과 요약

- `MKDirections`는 최단·최속 선택, 회피 조건 같은 경로 기준 옵션을 제공하지 않는다.
- `MKDirections.Request`는 출발지와 도착지만 받는다. 중간 경유지를 한 번에 넘길 수 없어 지점 N개는 요청 N-1회가 된다.
- Apple은 도보 경로 최적화 기준을 문서로 공개하지 않는다.
- 7개 지점 상한은 외부 API 제약이 아니라 MVP SPEC이 정한 값이었다.
- 따라서 "직진을 원했는데 다른 길이 선택되는" 문제는 API 옵션으로 못 고치고, 지점을 촘촘히 찍거나 자유 그리기로 우회한다.

---

## 2. 새로 추가할 기능 (범위 미확정)

### B1. 앱 안에서 GPX 수준 경로 생성

현재는 카카오맵 등 외부 서비스에서 만든 GPX를 가져온다. 앱 안에서 같은 수준의 경로를 만들 수 있어야 한다.

- B7의 자유 그리기가 이 요구의 상당 부분을 흡수한다. B7 완료 후 남은 격차를 다시 정의한다.
- GPX 내보내기가 필요한지 함께 판단한다.

### B2. GPS 기록 기반 코스 생성

Apple Watch 또는 iPhone GPS로 실제 달린 경로를 기록해 코스로 만든다.

- `RouteSource.RECORDED_GPS`는 데이터 모델에 이미 예약되어 있다.
- 선행 조건: 현재 `SPEC.md`는 위치 권한 요청을 명시적으로 제외한다. 스코프 변경이 필요하다.
- 위치 권한 등급, 백그라운드 수집, 배터리 정책, 개인정보 처리 방침을 함께 정해야 한다.
- watchOS 타깃을 추가할지 iPhone만으로 할지 결정이 필요하다.

### B3. 동적 날씨 표현

노을, 비, 눈 등 현재 날씨를 지도에 동적으로 표시한다. 태양과 구름이 움직이는 표현.

- 우선순위: 가장 마지막.
- WeatherKit 사용 가능 여부와 호출 한도를 확인한다.
- 성능, 배터리, Reduce Motion 대응을 함께 확인한다.

### B4. 커뮤니티 / 크루

러닝 크루 생성과 모집.

- 현재 `SPEC.md` 스코프 제외 항목(댓글, 팔로우, 소셜 피드)과 겹친다. 범위 재정의가 필요하다.
- 가입 승인, 권한, 신고와 차단 정책을 함께 설계해야 한다.
- 사용자 생성 콘텐츠가 늘어나므로 운영 도구도 같이 검토한다.

### B5. 코스 안내 (내비게이션)

선택한 코스를 따라 실시간으로 안내한다.

- 음성 안내("10m 앞 우회전"), 실시간 위치 표시, 경로 이탈 감지와 알림.
- 선행 조건: B2와 마찬가지로 위치 권한 스코프 변경이 필요하다.
- 보행자 턴바이턴 안내 데이터 확보 방안을 먼저 확인한다. Kakao 도보 API는 제휴 전용이고 MapKit은 도보 안내 음성을 제공하지 않는다.
- 자유 그리기 경로는 도로망 정보가 없어 턴바이턴 안내를 만들 수 없다. 이탈 감지만 가능하다.

---

## 3. 기존 기능의 남은 개선 (범위 미확정)

### B8-1. 요소 영상

사진 파이프라인이 안정된 뒤 검토한다.

- 저장소 설계(6.6)는 영상을 함께 고려해 두었으므로 저장 위치는 그대로 쓸 수 있다.
- 추가로 필요한 결정: 용량 상한, 인코딩과 트랜스코딩, 썸네일 추출, 재생 플레이어, 대역폭 비용.

### B8-2. 요소당 여러 장의 사진

현재 기준은 요소당 1장이다. 갤러리 형태가 필요한지 실제 사용을 보고 판단한다.

### B11. 자체 라우팅 엔진 검토

MapKit `MKDirections`는 출발지와 도착지 두 점만 받아서, 경유지가 있는 경로는 구간마다 별도 요청이 필요하다(지점 N개 = 요청 N-1회). 대부분의 라우팅 API(Google Directions, OSRM, Kakao)는 좌표 배열을 한 번에 받아 1회 요청으로 처리한다.

Mac mini에 OSM 기반 라우팅 엔진(OSRM, GraphHopper, Valhalla 중 하나)을 올리면:

- 요청 제한이 사라지고 지점 상한을 우리가 정한다
- 좌표 배열을 한 번에 넘겨 요청 1회로 끝난다
- 인도, 계단, 공원 산책로 등 보행 전용 경로가 OSM에서 더 풍부한 경우가 있다

비용: 지도 데이터 갱신 절차, 서버 메모리(라우팅 엔진은 상당량 사용), 경로 품질 검증.

검토 시점: 지점 상한을 25개보다 더 올려야 하거나 B1(앱 내 GPX 수준 경로 생성)을 본격적으로 할 때. Phase 9는 MapKit + 세그먼트 캐싱으로 충분하다고 판단해 지금은 도입하지 않는다.
